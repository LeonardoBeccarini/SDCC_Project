apiVersion: apps/v1
kind: Deployment
metadata:
  name: deviceservice
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deviceservice
  template:
    metadata:
      labels:
        app: deviceservice
    spec:
      containers:
        - name: deviceservice
          image: deviceservice:latest
          imagePullPolicy: Never
          env:
            - name: RABBITMQ_HOST
              value: "rabbitmq.rabbitmq.svc.cluster.local"
            - name: RABBITMQ_PORT
              value: "1883"
            - name: RABBITMQ_USER
              value: "myuser"          # 👈 aggiunto
            - name: RABBITMQ_PASSWORD
              value: "mypassword"      # 👈 aggiunto
